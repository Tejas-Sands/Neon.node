fastapi>=0.95.0
uvicorn>=0.20.0
pydantic>=2.0.0
requests>=2.28.0
beautifulsoup4>=4.12.0
instagrapi>=2.1.1
# 7.x tracks Microsoft's endpoint/DRM changes — older 6.x releases start
# 403-ing when the service rotates tokens (also: bumping this line rebuilds
# the CI venv cache, whose key hashes this file).
edge-tts>=7.0.0
faster-whisper>=1.0.0
certifi>=2024.2.2
urllib3>=2.0.0
